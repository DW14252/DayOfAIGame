using UnityEngine;

public class displayInfoPanels : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public GameObject[] infoPanels;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
